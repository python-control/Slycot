Known issues:

- if you get the glibc error "double free or corruption" you can try
	export MALLOC_CHECK_=0
  before executing python.
  
TODO:
 
- write unit tests, starting from converting the examples
- find a way to make L input argument optional in sb02od routine.
- make multiple wrappers for SB02OD for managing different modes.
